/*
	does the group already 'knowsAbout' > 0.1
	if not, can any crew member see the player.
	if so, apply the detection percentage
	if random(1)< detection percentage then apply knowsAbout 0.15
	do this for each crew until all have been sampled or knowsAbout increased.
*/

/*
	when are reinforcements called in ?  
	when an air patrol knows about a unit and 
	chance of spawning criteria are met e.g. 
	random(1) < chance of spawn.
*/

/*
	how are airdrops of reinforcements called in ?
	
*/ 